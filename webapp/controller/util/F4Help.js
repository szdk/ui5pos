sap.ui.define([
    ],
    function () {
        "use strict";
        return {
            f4help : (
                ODataModel,
                entitySetPath,
                filterFields,
                showFields,
                returnFields,
                
            ) => {

            }
        };
    }
);